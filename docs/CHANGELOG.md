# Changelog

Running list of what was built or changed, in order. **Append-only: add one line per meaningful addition or change; do not delete or edit past entries.**

---

[2026-03-05] - Initial Phase 1: Drizzle schema (v2 Solo), Neon client, clients module (list, detail, create, update, archive), Server Actions with Zod.

[2026-03-05] - PRD v2 Solo: removed users table, project_members, comments, activity_log; clients contact_name/contact_email/contact_phone; added settings table; invoice status without viewed; removed created_by/assignee/uploaded_by from schema.

[2026-03-05] - NextAuth v5 single-admin auth: credentials provider, ADMIN_EMAIL/ADMIN_PASSWORD_HASH, JWT session, login page, dashboard layout protection, SessionProvider, NavUser sign out.

[2026-03-05] - Sidebar updated to AgencyOS nav only: Dashboard, Clients, Projects, Tasks, Invoices, Reports, Settings; removed Users, Register, Login, 404/500, proposals; AppSidebar accepts session user.

[2026-03-05] - Removed register page and dashboard/users module; client detail Activity tab removed; Notes tab shows client notes; routes-config updated for v2 Solo.

[2026-03-05] - Created /docs folder: README, ARCHITECTURE, DATABASE, MODULES, API, DECISIONS, CHANGELOG, TODO.

[2026-03-05] - Added .npmrc with legacy-peer-deps=true so npm install works without --legacy-peer-deps (lucide-react vs React 19).

[2026-03-05] - Clients: no location/address on form; email optional, phone required; optional logo upload via ImageKit. Added lib/imagekit.ts, POST /api/upload, @imagekit/nodejs; ClientFormSheet uses /api/upload for logo and saves logoUrl.

[2026-03-06] - Settings: full Settings page (single page, no tabs). Four sections with cards — Agency Profile (name, email, website, VAT, logo upload via /api/upload scope agency-logo, address), Invoice Defaults (prefix, next number preview, currency, payment terms, footer), Branding (primary color picker + swatch), Account (admin email read-only, change password form with Zod validation, success toast only). actions/settings.ts: getSettings, updateAgencyProfile, updateInvoiceDefaults, updateBranding, changePassword; Zod schemas; upsert for settings row id=1. Schema: added agencyWebsite to settings table. Upload API: agency-logo scope and IMAGEKIT_AGENCY_LOGO_FOLDER. Settings layout simplified to children-only.

[2026-03-05] - Phase 2: Projects module. List page (card/list view, search, status and client filters, New Project Dialog). Detail page with tabs: Overview (edit modal, phases stepper), Tasks (Kanban with create/update), Invoices (list + New Invoice link), Files (placeholder), Notes (textarea + save). Server Actions: projects (create, update, delete, getProjects, getProjectById, getProjectTaskCounts, updatePhaseStatus, updateProjectNotes), tasks (getTasksByProjectId, createTask, updateTask), invoices (getInvoicesByProjectId), settings (getSettings). Breadcrumb: Projects > [Project Name].

[2026-03-06] - Settings page: single page with 4 sections (Agency Profile, Invoice Defaults, Branding, Account). Server Actions: getSettings, updateAgencyProfile, updateInvoiceDefaults, updateBranding, changePassword (validate only). Agency profile: name, email, website, VAT, logo upload (/api/upload scope agency-logo), address. Invoice defaults: prefix, next number, currency, payment terms, footer. Branding: invoice color (hex + swatch). Account: read-only admin email, change password form with success toast. Schema: added agency_website to settings table. Layout: no sidebar; single column.

[2026-03-06] - Phase 3: Invoices module. List page with summary bar (Total Invoiced, Collected, Outstanding, Overdue), filters (search, status, date range), table; autoMarkOverdue on load. New Invoice dialog: client, project (filtered by client), auto invoice number from settings, issue/due dates, SAR currency, line items with tax, notes from settings; Save as Draft / Create & Send. Detail page: formatted preview, Download PDF, Mark as Sent/Paid (dialog), Duplicate, Cancel, Edit (draft only). Edit page (draft only) with full form. GET /api/invoices/[id]/pdf with @react-pdf/renderer (agency logo, invoice_color). Schema: invoices.payment_method, currency default SAR. Actions: getInvoices, getInvoiceStats, getInvoiceById, createInvoice (auto-increment next number), updateInvoice, markAsSent, markAsPaid, cancelInvoice, duplicateInvoice, autoMarkOverdue, getNextInvoiceNumber.

[2026-03-06] - Dashboard Home: KPI cards (Revenue This Month with vs last month %, Outstanding, Active Projects, Overdue Tasks). Revenue last 12 months bar chart (Invoiced vs Collected) and Project status donut (Recharts). Three columns: Overdue tasks (up to 5, link to project), Upcoming deadlines (projects with end_date in 14 days), Recent invoices (last 5). Quick actions bar: New Project, New Client, New Invoice, New Task. All data from getDashboardData() (Drizzle); currency from settings; empty states per widget.

[2026-03-06] - Projects list: 3 view modes — Table, Gallery (default), Board. View switcher: 3 icon buttons (Table, LayoutGrid, Columns) grouped before "New Project"; active view highlighted. Table: DataTable with Company (avatar+name), Project name, Status, Deadline, Budget (SAR), Tasks progress, Actions (Edit, Delete). Gallery: grid 4/2/1; card cover = client logo or status-colored block with project initial; … menu on hover (View, Edit, Delete). Board: Kanban columns (Lead, Active, On Hold, Review, Completed, Cancelled) with colored dot and count; scrollable cards (name, client, deadline, budget, tasks). Search and status/client filters apply to all views. EditProjectDialog and deleteProject wired in list view.

[2026-03-06] - Converted entire app to Arabic RTL with Cairo font.

[2026-03-06] - Reports: Financial tab. Page with metadata title "التقارير", two tabs (التقارير المالية \| تقارير المشاريع والإنتاجية). Date range filter (هذا الشهر, الشهر الماضي, هذا الربع, هذه السنة, كل الوقت). KPI cards (إيرادات هذا الشهر + delta, إجمالي المحصّل, المستحق حالياً, إجمالي الفواتير هذا العام). Revenue chart (Recharts, Arabic months, الفواتير المُصدرة / المحصّل). Top 5 clients by revenue, last 8 invoices. Outstanding invoices table (pending only, days since issue, inline "تحديد كمدفوعة"). actions/reports.ts: getFinancialSummary, getMonthlyRevenue, getTopClientsByRevenue, getRecentInvoices, getOutstandingInvoices. RTL throughout, amounts as 1,000 ر.س.

[2026-03-06] - Delete (حذف) for Clients, Projects, Invoices. Row actions menu: added "حذف" option; clicking opens shadcn AlertDialog (title "هل أنت متأكد؟", module-specific Arabic description, إلغاء / حذف destructive). Clients: `deleteClient(id)` hard delete, CASCADE; redirect to /dashboard/clients. Projects: `deleteProject(id)` hard delete (was soft-delete), CASCADE tasks; refresh list. Invoices: `deleteInvoice(id)` only if draft or cancelled, else error toast "لا يمكن حذف فاتورة مُرسلة أو مدفوعة. قم بإلغائها أولاً."; CASCADE invoice_items. Schema already had ON DELETE CASCADE (projects→clients, tasks→projects, invoices→clients, invoice_items→invoices, files→client/project). Docs: MODULES.md and CHANGELOG updated. Root layout: `<html lang="ar" dir="rtl">`, body `dir="rtl"`, Cairo from next/font/google (arabic + latin). All UI copy translated to Arabic (navigation, clients, projects, tasks, invoices, dashboard, settings). Sidebar on right; dropdowns/popovers align start for RTL; breadcrumb and sidebar trigger icons mirrored. Numbers stay Western (0–9); dates DD/MM/YYYY; currency "amount ر.س". Docs: ARCHITECTURE.md notes RTL/Cairo/dir; CHANGELOG entry.

[2026-03-06] - Reports: Productivity tab (تقارير المشاريع والإنتاجية). KPI cards (المشاريع النشطة, مشاريع مكتملة هذا العام, المهام المتأخرة in red, معدل إنجاز المهام with progress bar). Donut chart توزيع المشاريع حسب الحالة (Recharts PieChart, Arabic legend/tooltip, status colors). Bar chart المهام المنجزة أسبوعياً (last 8 weeks, indigo). Table "حالة المشاريع الحالية" (project link, client avatar+name, status badge, due date red if passed, budget SAR, task progress bar, days remaining red/amber/green). Overdue tasks list with "تحديد كمكتملة" button (updateTask status=done), empty state "🎉 لا توجد مهام متأخرة!". New clients this year: count, 12-month bar chart (Arabic month names), last 5 clients. actions/reports.ts: getProjectsSummary, getProjectsByStatus, getWeeklyTaskCompletion, getOverdueTasks, getActiveProjectsWithProgress, getNewClientsPerMonth. ProductivityReportsTab client component. updateTask/updateTaskStatus revalidate /dashboard/reports. RTL, Western numerals. MODULES.md and CHANGELOG updated.

[2026-03-06] - File Manager (Phase 3). POST /api/upload accepts `folder` (ImageKit path) and returns { url, fileId, name, size, mimeType, filePath }. actions/files.ts: getFiles({ clientId?, projectId? }), createFile(data), deleteFile(id) — delete from ImageKit then DB. FileManager component: clientId/projectId scoped, "الملفات" title, "رفع ملف +" button, drag & drop, grid (4/2/1 cols) with image thumbnails (?tr=w-200,h-150,c-at_max), PDF icon (red), generic file icon, name/size/date, hover تحميل | نسخ الرابط | حذف; upload progress, parallel uploads, createFile after upload; delete AlertDialog then deleteFile; copy link toast. Wired on Client detail (الملفات tab) and Project detail (الملفات tab). RTL. MODULES.md Files section, CHANGELOG, TODO.md Files marked done.

[2026-03-06] - Tasks module (global): Full tasks page at app/dashboard/tasks/page.tsx. Header "المهام", view toggle كانبان | قائمة (default Kanban), "+ مهمة جديدة", filters (search, project "كل المشاريع", priority "كل الأولويات | منخفض | متوسط | عالي | عاجل", status in list view only). Kanban: 5 columns RTL (قيد الانتظار, قيد التنفيذ, قيد المراجعة, مكتمل, موقوف) with colored headers, "+ إضافة مهمة" per column, task cards (title, project link, priority badge, due date red if overdue, subtask count, … تعديل | حذف). List: RTL table (المهمة, المشروع, الأولوية, الحالة, تاريخ الاستحقاق, الإجراءات). New Task Modal: عنوان المهمة, المشروع, الوصف, الحالة, الأولوية, تاريخ الاستحقاق, الساعات المقدرة; إنشاء المهمة. Task Detail Modal: editable title/status/priority/description, subtasks with checkbox (toggleSubtask), "+ إضافة مهمة فرعية", حذف with AlertDialog. actions/tasks.ts: getTasks(filters), getTaskById, createTask (with estimatedHours), updateTask, updateTaskStatus, deleteTask (soft), createSubtask, toggleSubtask, getTasksByProjectId; all Zod-validated. Schema: indexes on tasks (project_id, status, parent_task_id). types: TASK_STATUS_HEADER_CLASS for Kanban column colors. Docs: MODULES.md Tasks section, CHANGELOG, TODO.md Tasks marked done.

[2026-03-06] - Mark-as-paid flow: "تحديد كمدفوعة" now opens a confirmation dialog "تأكيد استلام الدفعة" with تاريخ الدفع (date picker, default today) and طريقة الدفع (optional: تحويل بنكي | نقداً | بطاقة ائتمان | أخرى). markAsPaid saves **user-entered date** as paid_at (not server time), so revenue chart shows payments in the correct month. Applied on invoice list (… menu + status badge), invoice detail, reports outstanding table, client invoices tab. Shared MarkAsPaidDialog component. Invoice detail page shows تاريخ الدفع and طريقة الدفع when status is paid. actions/invoices.ts: markAsPaid paymentMethod optional; added credit_card; revalidate reports. Docs: MODULES.md, CHANGELOG.

[2026-03-06] - Financial reports: (1) Fixed "المحصّل" bar — getMonthlyRevenue now uses **paid_at** for collected (invoice paid in March appears in March collected) and **created_at** for الفواتير المُصدرة (invoiced). updateInvoiceStatus/markAsPaid already set paid_at when marking paid. (2) Expenses module: New table `expenses` (id, title, amount, category enum, date, notes, receipt_url, created_at); expense_category enum (software, hosting, marketing, salaries, equipment, office, other). drizzle-kit push. New page app/dashboard/expenses/page.tsx: title "المصروفات", "+ إضافة مصروف", summary bar (إجمالي هذا الشهر, إجمالي هذه السنة, أكبر فئة), filters (category dropdown, date range), RTL table (العنوان, الفئة badge, المبلغ, التاريخ, ملاحظات, إجراءات … تعديل | حذف). NewExpenseDialog: العنوان, المبلغ بالريال, الفئة (برمجيات | استضافة | تسويق | رواتب | معدات | مكتب | أخرى), التاريخ (default today), ملاحظات, إيصال (optional upload via /api/upload folder expenses/receipts). actions/expenses.ts: getExpenses(filters), getExpensesSummary(), createExpense, updateExpense, deleteExpense; all Zod-validated. Sidebar: "المصروفات" between الفواتير and التقارير (IconWallet). (3) Financial reports: New KPI card صافي الربح (collected − expenses for selected period, green/red). Revenue chart: third bar المصروفات (red/rose); tooltip shows all three; summary below: إجمالي المحصّل | إجمالي المصروفات | صافي الربح. getMonthlyRevenue now returns monthly expenses from expenses table. Category badge colors: برمجيات blue, استضافة purple, تسويق pink, رواتب amber, معدات orange, مكتب gray, أخرى slate. Docs: DATABASE.md (expenses table, expense_category), MODULES.md (Expenses section, Reports updates), CHANGELOG, TODO.md.
